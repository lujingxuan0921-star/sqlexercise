1.DBMS database management system是什么：专门用来管理、操作和维护这些数据的系统

DBMS的运作流程：
  在实际应用中，你（用户或应用程序）是不直接接触底层硬盘上的数据的。所有的操作都要经过 DBMS；
  用户/应用端： 你在网页上点击“购买”，或者在 CMD 里输入 SELECT * FROM shop;
  DBMS 接收并处理： DBMS 接收到你的请求，检查你的权限，翻译你的 SQL 语句，寻找最快的查找路径（优化器）；
  操作系统与存储： DBMS 帮你去硬盘里把对应的数据取出来，再整整齐齐地返回给你

DBMS的种类：
  HDB（层次数据库）；RDBMS（关系型数据库）；OODB（面向对象数据库）；XML数据库；键值存储系统

2.RDBMS的结构：
  客户端/服务器结构：客户端向服务器发送SQL语句，服务器在数据库内调取数据发送回数据

表的结构：
  二维：列为字段，行为记录，必须以行为单位进行读写

3.SQL语句的种类：
  DDL语句：CREATE;DROP;ALTER（针对数据库和表）
  DML语句：SELECT;INSERT;UPDATE;DELETE（针对表内的内容）
  DCL语句：COMMIT（确认对数据的操作）;ROLLBACK（取消对数据的操作）;GRANT（赋予权限）;REVOKE（取消权限）

具体SQL语句：
  创建database：CREATE DATABASE shop;
  创建tables：
  CREATE TABLE Product
  (product_id CHAR(4) NOT NULL,
  product_name VARCHAR(100) NOT NULL,
  product_type VARCHAR(32) NOT NULL,
  sale_price INTEGER ,
  purchase_price INTEGER ,
  regist_date DATE ,
  PRIMARY KEY (product_id));

数值型数据类型：
类型名称	占用空间与范围	适用场景与举例

INT / INTEGER	标准整数（约正负21亿之间）	
用户的唯一ID、商品库存量。
举例：user_id = 10086, stock = 50

TINYINT	极小整数（-128 到 127）	
状态值、性别、类型标号。（省空间利器）
举例：status = 1（1代表已支付，0代表未支付）

BIGINT	极大整数（应对天文数字）	
大型系统的订单号、全局流水号。
举例：order_no = 2026060713450001

DECIMAL / NUMERIC	精准小数（不丢失精度的定点数）	
钱、资产金额。（绝对不能用浮点数存钱，会有精度误差）
举例：price DECIMAL(10, 2) 表示总共10位，小数点后保留2位，如 199.99

FLOAT / DOUBLE	近似小数（非精准浮点数）	
科学计算、身高、体重、GPS经纬度。
举例：weight = 65.5, latitude = 31.2304

字符串型数据类型：
CHAR（定长字符串）：
特点： 空间是固定的。你定义 CHAR(4)，哪怕你只存一个字，它在硬盘里也死死占用 4 个字符的空间。但因为长度固定，数据库检索它的速度极快。
举例： 身份证号（18位）、手机号（11位）、MD5加密后的密码（32位）、性别（1位）。如 gender = '男'。

VARCHAR（变长字符串）：
特点： 空间是弹性的。你定义 VARCHAR(100)，如果只存了 3 个字，它在硬盘里就只占用 3 个字的空间（加上1字节的长度计数）。最常用，能极大节省硬盘。
举例： 用户昵称、商品名称、家庭住址。如 goods_name = '无线蓝牙耳机'。

TEXT（长文本）：
特点： 专门用来存放超级长的大段文字，最大可以存几GB。
举例： 博客文章正文、商品详细介绍、公告内容。如 article_content = '<body>欢迎来到我的博客...</body>'。

BLOB（二进制大对象）：
特点： 不存放文字，而是直接存放图片、音频、视频的二进制原始数据。
举例： 用户头像（虽然现代开发更倾向于把图片存到云存储，数据库只存图片网址 URL）。

日期型数据类型：
DATE：
特点： 只记录日期，不记录具体几点几分。
格式： YYYY-MM-DD
举例： 出生日期、入职日期。如 birthday = '1998-10-01'。

TIME：
特点： 只记录时间，或者时间间隔。
格式： HH:MM:SS
举例： 每天打卡时间、比赛耗时。如 check_in_time = '08:59:59'。

DATETIME：
特点： 日期和时间全都要。范围极大（公元1000年到9999年），与时区无关。
格式： YYYY-MM-DD HH:MM:SS
举例： 商品上架时间。如 publish_time = '2026-06-07 13:45:00'。

TIMESTAMP（时间戳）：
特点： 包含日期和时间，但它带有时区属性。如果换个时区看这行数据，时间会自动转换。范围较小（1970年到2038年）。
举例： 数据的创建时间、最后修改时间（常用于系统自动记录）。如 create_time = CURRENT_TIMESTAMP。
数据类型：

